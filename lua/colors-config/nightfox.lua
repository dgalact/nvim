local nightfox = require('nightfox')

nightfox.setup({
    fox = 'carbonfox', -- change the colorscheme
    styles = {
        comments = 'italic',
        functions = 'italic',
    },
})

-- load the configurations
nightfox.load()
